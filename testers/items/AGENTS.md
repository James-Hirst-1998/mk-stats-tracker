# Item Discovery Rules

- `item_finder.py` is the current working item-discovery script for this bucket.
- Keep new item experiments in `attempts/` until proven repeatable.
- If an approach fails, keep the script for reference and log it in `attempts/ATTEMPTS.md`.
