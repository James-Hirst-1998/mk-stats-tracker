# Dashboard rebuilt in TypeScript

Same numbers, rebuilt in TypeScript, React and Tailwind. Every stat is still
worked out from the race logs by fixed rules, so the same night always gives
the same page.
