# A page for every race

- Small widgets that open up, instead of full-width charts.
- Each race has its own page: time in each position, items, every hit and who
  threw it, every blue shell.
- A course whose lap traced badly can have it drawn by hand, so its replay
  runs the right way round.
