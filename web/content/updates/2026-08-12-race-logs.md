# Track a whole night

`tools/track.py` runs alongside Dolphin for a whole VS session and saves every
race as it finishes: positions, lap times, items, and every hit with what caused
it and who threw it.

`tools/report.py` reads a night back with no Dolphin running.
