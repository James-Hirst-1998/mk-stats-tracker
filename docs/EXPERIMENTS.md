# Experiments

The running log. Every attempt gets a line, including the ones that failed —
knowing which approaches are dead ends is most of what this file is for.
Corrections are appended as new dated lines rather than edited in place, so
the record of being wrong survives.

Started as an item-tracking log, which is why the early entries are all items.

## Open

- **Who is carrying a Thunder Cloud right now.** The cloud is a world object of
  type 14 and its `+0x6C` owner is whoever *won* it, which does not move when
  the cloud is passed on — daisy's cloud stays owner 8 while slot 10 is the one
  struck. Every byte in the object's first `0x400` was scored against the two
  labelled moments each cloud gives (the winner when it appears, the victim
  when it goes off, different in 4 of 9 clouds) and none reads both. The
  carrier is probably a pointer to a kart rather than a slot index.
- **The end of a Bullet Bill ride** is not visible in progress. Rate per second
  over 17 rides: some fall off a cliff (2.6 → 0.8, 9.4 → 1.6) and some never do
  (2.4 → 2.4 over the whole 14s), because a fast section looks the same as a
  bullet. Positions gained needs the ride's end, so it needs a real field —
  the same `KartMove` trip as mini-turbos. What does work is the other half:
  everyone the rider flattens is now credited to them.
- **A standing-still race clock does not mean the race is paused.** Over the
  seven recordings, with nobody ever pausing, the clock repeats on 4–19% of
  in-race snapshots in runs of up to 30. Not a slow emulator: those same
  recordings run at 20.7–23.6 frames per game-second against a 20 Hz recorder,
  which is real time. So it is stalls — the recorder's, or the game's — and
  they are long enough to look exactly like a pause. Whether that also happens
  when nothing is being recorded is untested, and a live session is the cheap
  way to find out: if the clock never repeats live, the check becomes viable
  after all. Until then, a pause needs a real flag.
- **The last 9% of hit naming.** 17 of 182 item-caused hits have no candidate
  despawn in the window. Most are bananas, where the sampling rate can miss a
  short-lived object. Not obviously worth chasing.
- **Item types 3, 6, 8, 10, 11, 13, 14** have pools but never spawned in seven
  recordings. Type 3 has capacity 14 and its handler calls into `KartItem`,
  so it is probably a held or dragged shell rather than a thrown one.
  2026-09-03: 3, 8 and 13 have since spawned, as dropped items - 3 is a
  Mushroom, 8 a Mega Mushroom, 13 a Bullet Bill. See the log. 6, 10 and 11
  remain unseen.
- **Picking a dropped item up off the road.** James did it in
  `20260903-194727-amy-2-player` race 1 and the log has no `hold` for it. Not
  a code gap: `hold` needs only `+0x08F` to go 20 -> item. Across three
  recordings every one of 216 holds follows a roulette and none of the 13
  dropped items was driven over, so whether the pickup writes `+0x08F`, runs
  a roulette, or writes neither is undecided. Needs a recording of a human
  driving over a dropped Star on purpose (the only dropped pickup that lasts,
  30s; Mushrooms and Goldens go in under a second), with the video. The
  dropped item is a type-4 object in the pools with the loser as owner, so
  the sample it leaves the pool is where to look. 2026-09-04: half answered.
  Where round the lap each `box` fires says a pickup off the road **does** run
  an ordinary roulette and is written like a box - see the log. So the
  question is no longer "is it written" but "is that one event really a
  pickup", and the way to settle it is still the same deliberate recording.
- **Falling off** has no direct read. "Lost ground with no damage" is a decent
  proxy and is not verified as one.
- **How many laps the race is** is not read anywhere. `+0x26` is the highest
  lap a racer has reached, not the length of the race, so a race log derives
  the count from whoever finished. `RaceConfig + 0xB68` is the settings block
  and a byte in it reads 3; every recording here is a 3-lap race, so "it reads
  3" is not evidence.
- **Which race of a VS sequence is in progress.** Same block, same problem: no
  recording here is part of a sequence, so nothing in it can be told from a
  constant. The block is now stored raw in every race log, so the next VS
  session James records answers it without a gigabyte recording.
- **The game's own VS points total** has not been located. A session adds up
  the published table from the finishing positions instead.
- **Quantity remaining in a triple.** The held id stays put while all three
  are thrown. Closed by decision, 2026-08-13 — see the log.
- MEM2 above `0x91800000` has never been captured. Nothing has needed it yet.

## Log

## Known Failed / Unstable Approaches
- Block+offset from player block: works in one race, breaks next race due to allocation changes.
- Single/short pointer-chain from position block to held-item byte: not stable; do not retry same chain strategy.
- Scanning MEM1 for pointers to per-race item addresses: no stable pointer path found.
- "Item used" transition matching (old->new): misses held/cart states (for example triple bananas while held), so not reliable.
- Neighbour-pointer scans near `0x809c27f8`: not proven stable across races/restarts.

## Entry Format (append for each new attempt)
- `YYYY-MM-DD`: method -> result -> keep/stop.
- `2026-03-01`: track-slot scanner (`testers/tracks/track_finder.py`) using rescan+intersect across track changes -> created, pending on-device validation across races/restarts -> keep.
- `2026-03-01`: adjusted track scanner to prefer byte-width matching + unaligned scans + width-1 fallback on zero-intersection -> intended to reduce false elimination -> keep.
- `2026-03-01`: removed argparse; switched track finder to one interactive run that tests widths 1/2/4 together and can enter watch mode inline -> keep.
- `2026-03-01`: replaced track finder with stable `(pointer_slot, offset)` discovery across samples (not absolute-address intersection) + in-script validation phase -> keep.
- `2026-03-01`: hardened pointer-slot tracker: stable-race polling gate + non-destructive filtering (do not zero-out on one bad sample) + optional safe intersection for large sets -> keep.
- `2026-03-01`: added `testers/tracks/track_path_tester.py` to validate discovered path `u8(u32(0x809bec9c)+0x70)` against user-picked next tracks with PASS/FAIL output -> keep.
- `2026-03-01`: hardened `track_path_tester.py` to ignore out-of-range transition values (e.g. 252) and require stable in-race polls before accepting next-track read -> keep.
- `2026-03-01`: switched track scripts to MKW course-slot codes (not cup-order indices), rebuilt finder with non-destructive `(slot,offset)` filtering, and made path tester generic (user-provided slot+offset) -> keep.
- `2026-03-01`: added `testers/tracks/track_pool_checker.py` to poll all 10 discovered candidate paths and report consensus/mismatches across race events -> keep.
- `2026-03-01`: validated 10/10 track path consensus across transitions (including Maple Treeway), documented pool in `testers/tracks/CONFIRMED_TRACK_PATHS.md`, promoted `u8(u32(0x809c27f8)+0x13)` into `source/read_race.py`, removed untrusted item read from source -> final.
- `2026-03-03`: added `testers/race/race_timer_probe.py` to find timer-like offsets (moving during race, frozen while paused) and provide live "timer_running" watch mode -> pending on-device validation -> keep.
- `2026-03-03`: updated `testers/race/race_timer_probe.py` with overlap-dedupe and interactive extra refinement windows (run/pause) to reduce aliases to a short candidate list before watch mode -> pending on-device validation -> keep.
- `2026-03-03`: updated `testers/race/race_timer_probe.py` to enforce extra pause/run validation cycles and watch all remaining candidates at 2s print intervals (instead of single fast stream) -> pending on-device validation -> keep.
- `2026-03-03`: added `testers/race/race_progress_state_tester.py` using `u32(u32(0x809c27f8)+0xb0)` to classify `not_in_race` vs `in_race_stopped` vs `in_race_running` with transition/2s logging -> pending cross-race validation -> keep.
- `2026-03-03`: added `testers/race/race_clock_probe.py` with moving vs unpaused-stationary vs paused windows to reject movement-only fields and find true race-clock candidates; includes refinement and 2s watch-all mode -> pending on-device validation -> keep.
- `2026-03-03`: added `testers/race/race_clock_cross_race_checker.py` to monitor the 4 race-clock candidates across menu/race transitions, report consensus running state, and flag divergence/unanimity -> pending cross-race validation -> keep.
- `2026-03-03`: added `testers/race/race_running_consensus_tester.py` using dual-signal consensus (`0xb0` + `0x144`) with debounce/ambiguous-hold to reduce flicker and track `not_in_race` vs `in_race_running` vs `in_race_stopped` across race transitions -> pending validation -> keep.
- `2026-03-03`: added `testers/race/race_running_primary_tester.py` using `0x144` as primary state signal and `0xb0` as diagnostic only, with debounce/hysteresis to avoid paused-state jitter from secondary signal -> pending cross-race validation -> keep.
- `2026-03-03`: tuned `testers/race/race_running_primary_tester.py` for higher sensitivity on small timer ticks (`DELTA_MIN=1`, wider window/hysteresis) and added active/quiet counters in output to debug paused/unpaused classification reliability -> pending validation -> keep.
- `2026-03-03`: switched `testers/race/race_running_primary_tester.py` to minimal log mode (print only `in_race=True/False` on changes/interval) to reduce noise during transition testing -> keep.
- `2026-03-03`: validation note: minimal `race_running_primary_tester.py` output is useful as a coarse race-screen gate (`in_race`) but not specific enough for paused vs in-progress decisions -> keep as gate only.
- `2026-03-03`: added `testers/race/race_in_progress_tester.py` to output only `in_race` + `in_progress` using `0x144` delta hysteresis (start-fast, stop-sustained) for paused/countdown/finish discrimination -> pending cross-race validation -> keep.
- `2026-03-03`: hardened `testers/race/race_in_progress_tester.py` with race-session start latch + stronger jitter filtering (`MOVE_DELTA_MIN=16`) and sustained stop window to reduce false flicker; keep `in_race` as coarse gate only -> pending validation -> keep.
- `2026-03-03`: validation outcome: `race_in_progress_tester.py` still failed to track user pause/start transitions reliably on-device; treat `0x144` delta method as unreliable for final in-progress state -> stop as primary approach.
- `2026-03-03`: added `testers/race/race_pause_flag_finder.py` to find a direct run-vs-pause byte flag (stable within phase, consistent across RUN/PAUSE cycles) in `0x80900000-0x80A00000` and watch it as `in_progress` -> pending validation -> keep.
- `2026-08-11`: strategy change -> stop discovering one field at a time in live races; record whole sessions to disk (MEM1 + MEM2 low window, page-delta + zstd) and test every later hypothesis offline against the recording. Added `testers/capture/` (`capfmt.py`, `probe.py`, `recorder.py`, `replay.py`). Format round-trip verified synthetically (sequential + random access + typed reads + fidelity check); pending on-device throughput probe -> keep.
- `2026-08-11`: probe on this Mac -> reads ~3 GiB/s, full 48 MiB image in ~16 ms, 27.6 ms work/frame at 20 Hz with 1 late frame in 120; set `RATE_HZ=20`, `CHUNK=0x4000`. ~2 GB per 3-min race. Note: changed pages/frame (~640) is near-constant across 10/20/30 Hz, so most churn is not game state (likely audio/video buffers) -> candidate regions to trim later -> keep.
- `2026-08-11`: first real recording (Wario's Gold Mine, 12 players, 4025 frames @ 19.3 Hz, 2.14 GB) -> `replay.py` verify PASS, 41/41 sampled frames reproduce live anchor reads, interval median 0.0502s, only 2 gaps >0.5s -> keep as validated recording.
- `2026-08-11`: `testers/capture/find_player_array.py` -> matched the local position trace across the whole race (5 addresses survive), then found a 12-slot array at `0x8111d234`, stride `0xc4`, local player at slot 0, holding a permutation of 1..12 in every sampled frame; frame 0 reads the starting grid `[12..1]` -> all-racer positions confirmed present in the captured regions -> keep.
- `2026-08-11`: open -> `0x8111d234` is a per-race heap address and the 0xc4 stride implies a 196-byte per-player struct; next step is a stable path to it plus mining other offsets in that struct (laps/checkpoint/timers). Needs recordings from separate Dolphin launches before anything is promoted.
- `2026-08-11`: four recordings captured (Wario's Gold Mine, GCN Peach Beach `0x10`, GCN Waluigi Stadium, Daisy Circuit), all verify PASS. Array found in all four at stride `0xc4`, local player always slot 0, but base moves per race (`0x8111a8b0`/`0x8111ae94`/`0x8111c558`/`0x8111d234`) -> confirms heap allocation, not static.
- `2026-08-11`: `find_stable_path.py` intersected every aligned word within +/-0x200 of the array base across all four recordings -> exactly one survivor: **`array_base = u32(0x809bd730) + 0x140`**. `validate_path.py` then checked it on 534/534 in-race sampled frames across all four recordings (including across a Dolphin relaunch) -> 0 failures -> SOLVED the "block+offset breaks next race" failure.
- `2026-08-11`: per-player struct fields confirmed in all four recordings: `+0x000` position (permutation of 1..12), `+0x006` lap (1..3), `+0x0b0`/`+0x0b4` float race progress in laps (~0.96 on the grid -> 4.0 at finish), `+0x0bc`/`+0x0c0` float fraction through current lap, `+0x054`/`+0x060`/`+0x06c`/`+0x088` vtable `0x808b2d44` and `+0x0a4` vtable `0x808b34a4` (static, identical across recordings, usable to locate the array by shape). `+0x00e` and `+0x012` are track-dependent counters (43/30 and 13/16) -> checkpoint-like.
- `2026-08-11`: open -> slot 11's progress floats (`0xb0`-`0xc0`) are exactly zero in every frame of every recording while its position and lap are valid, and the `0x54` vtable confirms slot 11 is a real array entry. Cause unknown; does not affect position/lap. Investigate before relying on progress for the 12th racer.
- `2026-08-11`: note -> recording named "peach-gardens" reads course `0x10` = GCN Peach Beach in the `read_race.py` table (Waluigi `0x12` and Daisy `0x09` both match). Confirmed by James as GCN Peach Beach -> recording name is a slip, the course table in `read_race.py` is correct.
- `2026-08-11`: `testers/capture/find_object_arrays.py` -> enumerated 12-element object arrays by finding a static (non-heap) pointer repeated 12x at constant stride; found ~20 arrays including the known player struct, but none with item-like fields, because an item inventory is plain data with no vtable -> keep as a general structure-finder, not an item method.
- `2026-08-12`: `testers/capture/find_item_array.py` v1 -> mask far too loose plus a Python inner loop over millions of hits; ran ~15h without output, and strides started at 4 so a stride-1 plain array was unreachable -> killed, rewritten.
- `2026-08-12`: `find_item_array.py` v2 (tighter filter: max<=20, 10-400 changes, 15-95% of frames back at the grid value; gather-based array check, strides from 1) -> 5577 candidate bytes, 446 "twelve-slot arrays", but all at stride `0x24` inside one contiguous block `0x8111df10-0x8111ea08` holding only values {0,1,3} -> a long per-object state array that the sliding 12-in-a-row test keeps re-matching, not items -> label-free item search is not discriminating enough, stop this approach.
- `2026-08-12`: `find_item_from_video.py` -> ground truth from the screen recording. MKW only draws the item box while holding something, and its bright yellow LEFT border is item-independent (present frames scored 98/32/65 yellow pixels, absent frames 0). Colour-distance on the whole box failed first because it keys on the icon (gold mushroom and star scored ~1250, red shell only 151) -> use the left border strip alone.
- `2026-08-12`: video/memory alignment -> reading the recorder's elapsed clock off the terminal in the video gave 9.5s, but it only refreshes ~1/s so it was 0.7s out, which broke sample points near run boundaries. Correct offset found computationally at **10.2s** by maximising agreement between "box drawn" and "byte != empty" over candidate offsets; ten independent candidates all agreed. Offset is per-recording; derive it, do not eyeball it.
- `2026-08-12`: item byte identified -> `0x8124c3db` in the frantic recording, matching icons read off the video at 7 independent times (Thunder Cloud 14, Triple Red 17, Triple Mushroom 5, Green Shell 0, Triple Bananas 18, Banana 2, Red Shell 1) against the Tockdom encoding.
- `2026-08-12`: item array shape -> **12 slots, stride `0x248`, local player at slot 0, values 0..20 with 20 = empty**. Stride 4 was a red herring: neighbouring bytes in the same per-player struct also pass an item-like filter.
- `2026-08-12`: `find_item_path.py` -> shape is specific enough to locate the array in the other three recordings with no video at all. Pointer intersection found nothing within +/-0x400 (matching the old "no stable pointer path" finding), but at +/-0x20000 four equivalent paths survive, all resolving the frantic recording to the video-verified `0x8124c3db`.
- `2026-08-12`: `validate_item_path.py` -> **534/534 in-race sampled frames across all four recordings (including across a Dolphin relaunch) pass** for all four paths. Canonical: **`item_base = u32(0x809c3648) - 0x227d`**. Equivalents: `u32(0x809c1904)-0x1959`, `u32(0x809c22b0)-0x60bd`, `u32(0x809c3660)-0x4971`. Items SOLVED.
- `2026-08-12`: papercut -> `recordings/` is written under `sudo` so it is root-owned; derived files go to `testers/capture/cache/` (git-ignored) instead.
- `2026-08-12`: superseded -> use the screen recordings as ground truth. Rather than classifying item icons, detect the frames where the local player's item box CHANGES (pixel diff on the HUD region), then find memory bytes that change at exactly those times and hold steady between. Same trace-match that cut 48M candidates to 5 for position. Needs `ffmpeg` installed.
- `2026-08-12`: CORRECTION -> `+0x0b0`/`+0x0bc` are NOT race progress / lap fraction. `check_progress_field.py` ranks racers by them and compares to position: 6-12% agreement across all four recordings, i.e. chance (1/12). A sweep of every offset/width in the `0xc4` struct found nothing except `+0x000` (position itself) that reproduces position order. The real progress value is not in that struct. Claim withdrawn from `source/read_race.py`.
- `2026-08-12`: CORRECTION -> `0x8124c3db` is NOT the held item. James spotted it live (it tracks the roulette, not what you end up holding). Frame-by-frame trace plus video frames at t=21.5-23.3s show the box plainly displaying a Bullet Bill while the byte reads 20 (empty) throughout. It is a pickup/award register: it carries the awarded item briefly then clears. Still useful - it yields 15 clean pickup events per race with plausible items.
- `2026-08-12`: `find_held_item.py` (require constant value across a whole held run) -> 0 survivors. `find_held_from_pickup.py` (require equal to the awarded value 0.25-3s after pickup) -> best 4/15, chance level, hits in code/rodata. Both stop.
- `2026-08-12`: direct icon match -> read 7 item icons by eye deep inside held runs and matched bytes against those exact ids. Collapses 48M to 1 after three icons, then 0. Scoring instead of hard AND: the single best byte in all captured memory is `0x8124c3db` at 5/7, failing (reading empty) at two moments where the box clearly shows an item. So no byte in the captured regions equals the displayed item id persistently.
- `2026-08-12`: open -> held item is either (a) outside the captured regions (MEM2 is only recorded from `0x90000000-0x91800000`; `0x91800000-0x94000000` is NOT captured), or (b) stored under a different internal encoding from the display ids. Next: search for a field whose CHANGE TIMES align with box appear/disappear rather than one whose value equals the id - that finds a differently-encoded field and needs no new recording.
- `2026-08-12`: `find_progress.py` -> stop matching progress by VALUE RANGE, match it by SHAPE. Scanned every 4-byte-aligned float in all 48M for "never decreases across 60 samples spread over the race, actually moves, travels 1.5-48 units". 24 survivors: **12 racers x 2 adjacent floats at stride `0xc4`**, starting 0.962..0.998 (the starting grid, staggered) and ending ~4.0 (3 laps + 1). Race progress SOLVED.
- `2026-08-12`: CORRECTION -> the player array starts **`0x14` earlier** than this repo assumed. Real base is `u32(0x809bd730) + 0x11c`-ish, not `+0x140`. That off-by-one struct is exactly why `check_progress_field.py` measured chance: reading `+0x0b0` from the old base is `+0x000` of the NEXT racer, so it ranked everyone by their neighbour's progress. Position and lap were unaffected because they were found empirically at the shifted base.
- `2026-08-12`: `validate_progress.py` -> ranking racers by progress reproduces reported position **97.8/98.5/98.4/99.1%** per racer, whole-grid-exact 96/98/96/99%, and sits inside `[lap, lap+1)` 92-98%, across all four recordings. Residual is the game's own position smoothing.
- `2026-08-12`: `score_held_global.py` -> stop intersecting, start scoring. Requiring a byte to match the video at EVERY sampled frame kills the true answer on one frame of misalignment. Scoring by agreement instead is robust. First run used "item box drawn" as the target: top hits were HUD animation floats (`0x813dc204` = top byte of `-1.0f`), 99.4%, local-player only.
- `2026-08-12`: CORRECTION -> "item box drawn" is the WRONG target. Extracted 12 video frames across one pickup: the box is drawn throughout the roulette, cycling through random icons for ~3.5s, and only then settles. During the spin the inventory is empty, so the real field disagrees with "box drawn" on ~24% of the race and lands below the noise floor. This is why every earlier item search failed.
- `2026-08-12`: item-struct `+0x004` is the **roulette result**, not the held item. It names the item correctly and turns on the instant the box breaks - but its runs are 3.55/3.57/3.55/3.57s for the local player and 1.15s for every AI. A fixed lifetime, not a player deciding when to throw. Useful in its own right: it reveals what the spin has already chosen ~3.5s before the player sees it.
- `2026-08-12`: `score_held_committed.py` -> rescored against "box drawn AND roulette finished", dropping spinning frames entirely rather than labelling them. **`item_base + 0x01c` = the committed held item.** Runs start exactly when the roulette settles and last 2.3/7.4/1.0/7.1/2.7/1.7/0.5/1.2s - real behaviour, not a timer. Held item SOLVED.
- `2026-08-12`: `validate_held_item.py` -> `+0x01c` gives a clean item-id alphabet for **12/12 slots on all four recordings**, with plausible per-racer hold fractions. `0x813dc21b` is the same value in the HUD object (local player only) and keeps showing an item during Bullet Bill's ride, so prefer `+0x01c` for "used".
- `2026-08-12`: public RE as a candidate source -> SeekyCt's `mkw-structures` documents `RaceinfoPlayer`. Its offsets line up with what was found here once the base correction above is applied, and it named fields not yet found. Verified locally, PAL differs in two places: the two `Timer*` sit at `+0x3c`/`+0x40` (doc says `+0x38`/`+0x3c`), and `Timer` carries a vtable at `+0x00` so its fields are minutes `+0x04` u16, seconds `+0x06` u8, millis `+0x08` u16, set-flag `+0x0a`.
- `2026-08-12`: lap and race times SOLVED, from the game's own clock. `lapFinishTimes` is cumulative, one Timer per lap, with `+0x0a` reading 0 until that lap is done. Cross-checked: slot 0's lap 1 on Daisy reads `0:35.519` against `0:35.441` measured independently from capture timestamps. `+0x2c frameCounter` is a per-racer 60Hz clock that freezes on finishing; `+0x30 framesInFirst` is time spent leading.
- `2026-08-12`: race clock -> no separate path needed. `find_timer.py` found two 60Hz families (a session-long counter at static `0x8038-0x8042`, and race-length counters in the heap) and `find_timer_path.py` found no delta from the player base that survives all four recordings - but `RaceinfoPlayer +0x2c` makes that search unnecessary.
- `2026-08-12`: failed, do not retry -> `find_held_array.py` (12 small-alphabet piecewise-constant bytes at a constant stride, searched globally) drowns in noise; its stride detection reports runs at 0xfd8/0xf78/0xea0 that are artefacts of a dense block at stride 0x138. `find_held_by_pickup_times.py` (score bytes by changing at all 15 roulette-start or roulette-settle instants, windows 0.4s and 1.2s) only ever surfaces the roulette machinery itself: `0x8124c3xx`, RNG state at `0x81409xxx`/`0x813cd5xx`, and the static pointer `0x809c283c` which is non-zero exactly while the spin runs and points into MEM2.
- `2026-08-12`: open -> quantity remaining for a triple item. The held id stays put while the three are thrown, so throws inside a triple are not counted. `+0x008/+0x00b/+0x020/+0x094/+0x193/+0x230` were checked and none reads 3 while a triple is held.
- `2026-08-12`: open -> hit attribution (who hit whom with what) not started. Everything above came from offline analysis of existing recordings; this one probably needs a recording made specifically for it.
- `2026-08-12`: hit detection works without a new field. A struck racer stops advancing, so `raceCompletion` rate collapses against their own median pace (`find_hits.py`). Three predicted hits checked frame by frame on video: a lightning bolt flattening the kart at 174.8s, a white-out, and a spin-out with stars. Detection good, attribution not.
- `2026-08-12`: **Blue Shell hits VERIFIED on video.** Slot 2 threw a blue at mono 62.5s while slot 0 was P1; the stall detector fires at 68.1s; video at 57.2s shows the shell diving in, 57.9s the white-out, 58.6s the kart thrown into the air. Flight time 5.6s. Rule now in `find_events.py`: blue thrown -> the racer in P1 has their first stall 4-10s later. A 3.5s lookback misses every blue shell.
- `2026-08-12`: Lightning detected as a field-wide event - an item leaves someone's hands and >=7 racers stall within 3s. Correctly names all 12 racers at 174.4s in the frantic recording.
- `2026-08-12`: `find_countdown_timers.py` -> hit-type timers exist and have an unmistakable shape (rest at 0, jump, decrement once per game frame). Found **12 separately-allocated player objects** in the 0x8113-0x811f heap, each carrying the same five countdown fields at `+0`, `+4`, `+0x38`, `+0x78`, `+0x7a` from the cluster start. Not at a constant stride, so they need a pointer array.
- `2026-08-12`: failed so far -> mapping those objects to racer slots. `find_player_objects.py` finds 48 runs of >=12 player-heap pointers; the one it picks (`0x809c5c54`, stride 0x28) is the wrong table. Two arrays with kart-sized gaps look right (`0x8110fcdc`, `0x81125b10`) but their targets do not sit at a consistent offset from the detected clusters, so the sub-object boundary is still wrong. Not blocking: hit detection already works without it.
- `2026-08-12`: `RaceinfoPlayer +0x21` is NOT respawn in PAL, whatever the doc calls it. Traced across all four recordings: roughly three spans per racer per race, each about a lap long. Falling off the track cannot be validated from these recordings anyway - James never fell off, and the AI did not either on these tracks. Needs a recording made for it.
- `2026-08-12`: scoring bytes against progress stalls to find a damage field (`find_damage_state.py`) tops out at 91.3% where the base rate is 86.5%. Stalls also come from walls, grass and bad lines, so the label is too noisy. Do not retry without a cleaner label.
- `2026-08-12`: fifth recording `20260812-111655-yoshi-falls-fall-off` (DS Yoshi Falls, 100s) verifies USABLE, 41/41 fidelity, and every path in `source/read_race.py` replays correctly on it - a fifth separate Dolphin launch. Lap splits and finish times came out for all 12 racers.
- `2026-08-12`: **falling off is visible in raceCompletion.** Lakitu puts you back at the last checkpoint, so completion goes BACKWARD. In the Yoshi Falls recording only slot 0 reverses, twice: -0.078 at mono 91.2 and -0.025 at mono 33.5. Video at the 11.0s offset for this recording confirms the first: kart leaves the cliff at video 79.6s, hits the water at 80.2s, screen wipes at 81.2s. The -0.025 shows normal driving, so it is not a fall.
- `2026-08-12`: reversal magnitudes across all five recordings fall into three bands - about 1.0 (the lap counter wrapping, not a rescue), 0.06-0.08 (three events in five races, one video-confirmed), and a 0.03 band that the video shows as normal driving. `falls()` in `find_hits.py` therefore takes 0.05 < drop < 0.5. One confirmed event only; needs a second recording to firm up.
- `2026-08-12`: `find_offroad.py` -> searching for a u16 that is non-zero across the confirmed fall window and zero the rest of the race leaves several hundred addresses tied at 100%/100%, almost all in the static `0x809f-0x80a3` block. One event cannot discriminate. Retry only with a recording containing 3-4 falls, and intersect.
- `2026-08-12`: LIMITATION, worth stating plainly -> stall-based hit detection is sensitive but not specific. It cannot tell an item hit from driving onto grass or into a wall. On Yoshi Falls, where James was 12th and off the racing line, it reports 8 hits where he recalls one. On the frantic recording its named events (blue shell, lightning) are all correct, but the unnamed "hit" count is an upper bound, not a count of item hits.
- `2026-08-12`: bugfix -> `stalls()` dropped any stall still running when the recording ended, and counted the stationary grid before the lights as a hit for every racer. Both fixed; `race_start()` added.
- `2026-08-12`: sixth recording `20260812-113132-mushroom-gorge-clean-but-fall` (Mushroom Gorge, 178s, driven clean on purpose) verifies USABLE, 41/41.
- `2026-08-12`: **the screen recordings carry a usable voice track.** Game audio sits at RMS ~227 while James's narration peaks at 3500-10500, so speech bursts come out of a simple envelope threshold with no transcription. 15 bursts in the Mushroom Gorge recording. This is a reusable labelling tool - `ffmpeg -ac 1 -ar 8000 -f s16le`, 50ms RMS bins, threshold 3.5x the 60th percentile.
- `2026-08-12`: video/audio offset for that recording is **4.0s** (mono = video + 4.0), from the folder timestamp vs the .mov timestamp, and confirmed by three of the local player's five completion reversals landing within 1.2s of a speech burst.
- `2026-08-12`: CORRECTION -> completion reversal is NOT specific to falling off. All five of slot 0's reversals in Mushroom Gorge were checked frame by frame on video: **99.3s and 152.5s are falls into the void, 171.8s ends in the rescue white-out, but 17.6s and 134.2s are item hits** with sparks visible round the kart. Magnitudes overlap across causes - 0.024 is a fall here while 0.025 was normal driving in Yoshi Falls - so size cannot separate them. `falls()` renamed `lost_ground()`, threshold lowered 0.05 -> 0.02, and the claim is now "put back down the track", not "fell off".
- `2026-08-12`: `find_kart_objects.py` -> **three vtables each occur exactly 12 times in the player heap: `0x808cb6b8`, `0x808c99bc`, `0x808c9d74`**, at fixed offsets from one another. Scanning for one locates the twelve kart objects in any race with no pointer path. Confirmed on the Mushroom Gorge recording.
- `2026-08-12`: failed -> hit timers are not inline in the kart object. `read_kart_timers.py` finds only 4 countdown offsets within 0x2000 of a base, and mapping object -> slot by stall overlap is flat at 11-28%, i.e. no signal. `find_kart_subobjects.py` walks the 46 pointer slots populated in all twelve karts and every "countdown at the same offset" hit is the same two churn counters (values 1..8) reached via different paths. Do not retry the pointer walk without a tighter label.
- `2026-08-12`: best result so far on hit type -> using three video-and-voice-confirmed hit windows for slot 0 (mono 24.6-27.6, 34.9-36.4, 40.0-41.8) and 483 clean samples at least 4s from any stall or setback, a strict intersection cuts 24M u16 fields to **65** that are non-zero in every hit window and zero at every clean sample. All but one are static (`0x8039-0x80a4`, most likely sound-effect voices). The one heap address, `0x8115ad22`, tracks slot 0's events well (fires at 25s, 33s, 40s, 110s) but is NOT replicated per kart - reading the same offset in the other eleven karts gives constants - so it is local-player-only, probably rumble or HUD state.
- `2026-08-12`: open -> hit TYPE. The 65-candidate intersection is the live lead. What would break it open is more hits with the ITEM NAMED out loud and well separated in time, because a field whose VALUE differs by item is a far stronger constraint than one that is merely non-zero.
- `2026-08-12`: seventh recording `20260812-120158-dessert-hills-lots-happening` (DS Desert Hills, 158s, starting 12th) verifies USABLE, 41/41.
- `2026-08-12`: **hit type is solved, and not by searching.** The community's "Item Damage Type Modifier" Gecko codes name the PAL call sites that set the damage type. Those addresses are all in MEM1, which the recorder captures, so the game's own code can be disassembled straight out of a recording. `0x805808c4` is `li r4,10; bl 0x80590d5c` (shock), `0x805811a8` is `li r4,11` (POW), `0x805808bc` is `li r4,17` (Thunder Cloud) - the three constants match the published enum exactly, which validates the table before a single byte is scored.
- `2026-08-12`: `0x80590d5c` is a thunk: `proxy->[0]` (accessor), `->[0x2c]` (sub-object), vtable at `sub+0x0c`, slot 3. Resolving that statically - group every pointed-at heap object by the word at `+0x0c` and keep the classes with exactly twelve instances - gives secondary vtable `0x808b5008` whose slot 3 is `0x805675dc`. That is the damage handler.
- `2026-08-12`: **`sub + 0x1C` is the current damage type, s32, `-1` when the racer is not hit.** From `0x805675dc`: `stw r22,28(r21)` writes it (r22 = the type argument), `li r3,-1; stw r3,28(r21)` clears it, `mulli r3,r22,12` indexes a per-type table at `0x808b4c58`, `sth r5,246(r21)` stores that entry's priority at `sub+0xF6`, and `stw r6,192(r21)` keeps the entry pointer at `sub+0xC0` after the hit ends, so the LAST damage type survives the hit.
- `2026-08-12`: the pointer path re-uses something already proven. KartItem is a KartObjectProxy, so `accessor = u32(kartItem)` and `sub = u32(accessor + 0x2c)`. All twelve addresses reached this way match the twelve found independently by vtable `0x808b5008`, 12/12.
- `2026-08-12`: CORRECTION -> `read_race.py`'s item base was `u32(0x809c3648) - 0x227d`, a fitted constant. The game's own path is `u32(u32(0x809c3618) + 0x14)`, which is `0x73` lower, so the same bytes are `+0x077` (roulette) and `+0x08F` (held). Switched to the code-derived path; the held/roulette reads are byte-identical.
- `2026-08-12`: `find_kart_objects.py`'s "three vtables each occurring exactly 12 times" was NOT the kart objects. `0x808cb6b8` and friends are scene nodes - their `+0x00` is a vtable but the chain `[0]->[0x2c]` is nonsense, and 37 different classes have exactly twelve instances in a race, so a 12-count alone proves nothing. That is why every timer hunt hanging off it failed.
- `2026-08-12`: validated across all seven recordings, i.e. seven separate Dolphin launches: damage values never leave `-1..17` for any racer at any frame. `testers/capture/validate_damage.py`.
- `2026-08-12`: cross-check with no video at all (`testers/capture/cross_check_damage.py`): **254 of 274 item-caused hits (93%) have a matching item use within 12s**, read from a completely independent field. The two field-wide items are exact - a Lightning marks every racer except the one who used it in the same frame (11 victims in Yoshi Falls where slot 0 used it, 11 in Desert Hills), and a POW marks only the racers ahead of the user (5 and 6). The 20 residuals are all bananas lying on the track longer than the window, or Thunder Clouds, which pass between karts.
- `2026-08-12`: independent confirmation from James's own account - Yoshi Falls, "I fall off and get hit once", gives **exactly one** damage event for slot 0 (Mega Mushroom crush at 73.0s). Mushroom Gorge gives **exactly two**, matching the two of five completion reversals that the video showed as item hits.
- `2026-08-12`: CORRECTION -> the stall-based hit detector in `find_events.py` is wrong in both directions, and the damage field proves it. On Desert Hills it reports 11 hits for slot 0; the game records 10, and only 4 of them line up. It fires on 49.6/57.8/63.6/67.3/73.5/76.5/151.3s where no damage occurred, and misses the Lightning at 98.3s, the banana at 102.2s and the shell at 105.2s outright. Stop using stalls to detect hits.
- `2026-08-12`: CORRECTION -> the 65-candidate intersection from the previous round was built on bad labels. Voice bursts plus stalls put slot 0's Mushroom Gorge hits at 24.6/34.9/40.0s; the game says the only two hits in that race were at 32.0s and 81.1s. None of the three windows was a hit. Do not revive that line.
- `2026-08-12`: damage types 0, 2, 7, 10, 11, 13 and 17 are the item-caused ones. 1, 3, 4, 5, 6, 8, 9, 12, 14, 15 and 16 are track hazards or contact with a boosted kart, and are now reported separately. DS Desert Hills produces both 1 (Pokey) and 9 (Fire Snake), which is exactly what that track contains - a further check on the table.
- `2026-08-12`: Bob-omb and Blue Shell both write damage type 7, so the damage field alone says "launched" and no more. The item field separates them: across all seven recordings, **21 of 23 launched hits follow a Blue Shell use by 3.9-6.8s**, and a single shell catches two or three karts within half a second (Mushroom Gorge 71.9/72.1/72.4s all from slot 6's shell at ~67s; frantic 115.8/116.0s from slot 5's at ~110.9s). A Bob-omb goes off within about 2s of being dropped. `read_race.py` names it when exactly one of the two fits; otherwise it stays "Bob-omb or Blue Shell". `testers/capture/name_blue_shell.py`.
- `2026-08-12`: this also corrects the old blue-shell claim - it does NOT only hit P1. The explosion catches whoever is nearby, which is why the P1-only rule in `find_events.py` missed victims.
- `2026-08-12`: **world item objects.** The collision loop at `0x805725e8` walks `ItemDirector + 0x264 + i*4`, reading `+0x04` (item type) and `+0x6c` (owner). The type indexes a table of 3-word member-pointer descriptors at `0x808b5468` with the function at `+8`, so `getDamageType` for type t is `u32(0x808b5470 + t*0xc)`. Learning the enum by watching which object spawns after a known item use gives type 1 = Red Shell (5/6), 2 = Banana (25/33), 5 = Blue Shell (1/1), 7 = Fake Item Box (5/5); 0 = Green Shell and 9 = Bob-omb follow from the handlers they resolve to. `analysis/find_item_objects.py`.
- `2026-08-12`: caution on the above -> at most **3** objects were ever live at once across seven recordings, and spawn counts (17-59) run at about half the item uses (34-109). Some of that is items that never create a world object (mushrooms, stars, bullets, lightning, POW), but not all of it. The array bound of 16 and the despawn detection both need checking before this is used to name a hit.
- `2026-08-12`: attribution is NOT available from the victim's side. The collision code calls the damage function with `r8 = 12` - the "no attacker" sentinel - at both call sites (`0x805726e0` and `0x805727a8`), so nothing about who fired it reaches the damage sub-object. Confirmed by disassembly, not by failing to find it.
- `2026-08-12`: damage type 3 is "Star, Cow or CM Car" in the published table, but it fires repeatedly on Mushroom Gorge, which has neither cows nor cars. In practice it is being rammed by a kart on a Star. Renamed to "a boosted kart, cow or car".
- `2026-08-12`: repo restructured. `source/read_race.py` split into `mkw/` (addresses, names, reader, events), the capture harness into `mkw/capture/`, entry points into `tools/`, evidence-producing scripts into `analysis/`, everything exploratory into `lab/` by topic. Verified by replaying the real reader over a recording before and after: byte-identical, 210 events on Mushroom Gorge.
- `2026-08-12`: `lab/ppc.py` -> a small PowerPC disassembler, so reading the game's own code out of a recording is repeatable instead of done by hand. The damage-type work proved that method; this makes it a tool. Only the instruction forms MKW actually uses are decoded, anything else prints `.long`.
- `2026-08-12`: **CORRECTION -> `ItemDirector + 0x264` is not a list of the items in the world.** Disassembling `0x80799cac` (the function whose return value bounds the collision loop) shows it iterating a manager and packing the objects near ONE kart into that buffer, `stw r30,612(r3)`, capped at 16 by `cmpwi r29,16`. The collision loop then consumes it for that kart. So the "at most 3 objects alive at once" caution from earlier today was measuring a per-kart proximity buffer, and the type-enum votes built on it (red 5/6, banana 25/33) were counting whatever happened to be near a kart.
- `2026-08-12`: the manager that buffer is filled from, `*(0x809C2EF0)` walked by `0x80785df4`, is a per-frame registration list - `lab/items/find_live_items.py` and `trace_obj_count.py` read it correctly and it is empty on 90% of sampled frames (3675 of 4080 in the frantic race). Not usable at 20Hz. Do not retry.
- `2026-08-12`: `lab/items/find_item_vtables.py` -> collected the item object vtables from the few frames where that list is populated, then scanned MEM1 for them: **42 instances at the same addresses on every frame of the race**, so the game keeps a fixed pool. `find_item_pool.py` split them into three constant-stride runs, one per class - green shells stride 0x1FC, red 0x310, bananas 0x1B4 - which means an object's address alone names its type.
- `2026-08-12`: **item object pools SOLVED.** `dump_item_director.py` found the table those runs hang off: `ItemDirector + 0x48`, stride `0x24`, one entry per type, `+0x00` type index, `+0x04` object array, `+0x08` capacity, `+0x10` live count. `probe_pool_fields.py` picked out the live count by watching all nine words move (0 -> 3 on a Triple Green Shell, 3 -> 4 when another green was thrown); `probe_live_flag.py` showed the live objects are `array[0..live-1]` densely packed, all three carrying owner 8 for slot 8's triple. Peak 22 live in the frantic race against the old read's 3, and 106 spawns against 109 item uses.
- `2026-08-12`: type enum settled properly, `lab/items/track_pools.py`: 66/66 green, 65/65 red, 163/163 banana, 15/15 blue, 29/29 fake item box, 6/6 bob-omb. Same method as before, but on the real pools it is unanimous rather than 5/6.
- `2026-08-12`: `lab/items/read_damage_handlers.py` -> disassembled `u32(0x808B5468 + t*0xC + 8)` for every type. 0 and 1 `li r3,2`, 7 `li r31,2`, 2 `neg r3,r0` (0 or -1), 5 and 9 `li r3,7` gated on a byte at `+0x334` and `+0x1D8` - which is exactly the pair the collision loop special-cases. So damage 2 is object type 0, 1 or 7 and nothing else. Type 5 has capacity 1 and type 9 capacity 3, which is what settles blue shell vs bob-omb.
- `2026-08-12`: **the despawn lag is fixed, and that is what makes the match work.** `lab/items/match_hits.py` first tried a symmetric 0.25s window and got 12 of 182. Measuring the lag instead of assuming it: for damage 0 and 2, 131 of 182 nearest despawns land in 0.25-0.40s with the mode at 0.30-0.35 (20 game frames, the break animation) against a flat background; for damage 7 it is 1-2s, the explosion. A window of 0.20-0.45s / 0.60-2.50s follows from the measurement, not from taste.
- `2026-08-12`: **hit naming SOLVED.** `analysis/name_hit_items.py` names **165 of 182 item-caused hits (91%)** across all seven recordings - 78 bananas, 31 red shells, 20 fake item boxes, 20 blue shells, 13 green shells, 3 bob-ombs - and gives the thrower, which the damage path deliberately does not carry (`r8 = 12`). Independent check: 149 of those can be traced back to the throw that spawned the object, read from the held-item field via a different path, and **149 agree, 0 disagree**.
- `2026-08-12`: promoted into `mkw/`. `reader.read_world_items()` now reads the pools and fails closed (every pool entry states its own type index and every object repeats it). `events.Race` holds a hit back until its despawn window has passed rather than reporting it twice, so a shell hit appears 0.45s late and an explosion 2.5s late. Across the seven recordings the live reader now leaves only 8 of 335 hit lines saying "Shell or Fake Item Box" or "Bob-omb or Blue Shell".
- `2026-08-12`: `analysis/find_item_objects.py` kept but marked superseded in its own docstring - it is the record of the `+0x264` mistake, not a thing to run.
- `2026-08-12`: **who each racer is, SOLVED, and the lead came from code already disassembled for the item work.** The collision routine at `0x80572614` reads `*(0x809BD728)` and indexes it with `mulli r0,r23,240`, so there is a 12-entry array of `0xF0` structs on a static - which is where a character id would be. That static points into MEM2, which the recorder captures but `lab/players/dump_race_config.py` first rejected with a MEM1-only range check.
- `2026-08-12`: two false starts on the array's phase. Guessing that the players began one stride into the struct gave a clean 12..1 grid column but left slot 11 reading junk in every other field. `find_config_array.py` then found the phase from the data - every byte column at stride `0xF0` that is a permutation of 1..12 - and put the grid at `cfg + 0x108`, with a second copy `0xBF0` later, which is RaceConfig's second scenario.
- `2026-08-12`: settled it from code instead. `find_config_readers.py` scans MEM1 for every `lwz` with immediate `0xD728` (1285 of them) and keeps the ones near a `mulli ...,240`. `0x8052880C` gives it exactly: `lbz r4,36(r3)` is the racer count and `addi r6,r3,40` the array base, so racers are at `cfg + 0x28` stride `0xF0`, and `lwz r3,16(r3)` is the type field the game compares against 0 and 2. That puts the grid at struct `+0xE1`, character `+0x0C`, vehicle `+0x08`, type `+0x10`, team `+0xCC` - and `+0xCC` is the same address the collision code reads as `+0xF4` from the pointer, which agrees.
- `2026-08-12`: ground truth for the character ids turned up in the recordings themselves. `meta.json` notes say "birdo, mach bike, starting 12th" on three of them and "luigi ... mach bike" on two, and slot 0 reads character 17 / 7 and vehicle 22 accordingly. A video frame from the frantic recording shows the kart is visibly Birdo and carries a nameplate reading "Funky Kong" over the racer whose id is 22.
- `2026-08-12`: the vehicle table is not confirmed by name beyond Mach Bike, so it is checked structurally instead. MKW only lets a racer pick a vehicle of their own weight class, and in the vehicle ordering the class is `id % 3`, so every racer is one constraint tying the character table to the vehicle table. `analysis/validate_racers.py`: **84/84 agree**, 17/17 note labels match, characters never repeat, exactly one non-CPU and it is always slot 0.
- `2026-08-12`: CORRECTION -> `+0xE0` is not the starting grid, though it reads identically in a first race, where the grid is the slot order reversed. The two recordings that were not a first race disagree, and `+0xE1` is the one matching the notes.
- `2026-08-12`: **bug, reported by James -> the live event log kept restarting from 0:00 mid-race.** Cause: `read_players` refuses to return a snapshot where the twelve positions are not a permutation of 1..12, which is right, but live a snapshot is ~300 separate reads of a game running at 60Hz, so one occasionally lands mid-update and sees two racers in the same position. `Race.update(None)` treated that single frame as the race ending and wiped the log. Confirmed not a path problem: replaying the real reader over all seven recordings gives **0 dropouts in 21,926 in-race frames**, because a recording is one consistent image and cannot tear. Fixed by requiring `DROPOUT = 20` unreadable frames in a row (1s at 20Hz) before resetting, and `tools/live.py` now holds the last good frame through a short gap instead of flashing "waiting for a race".
- `2026-08-12`: **bug, reported by James -> the clock starts running over the track flyover, before the countdown.** It was right: `RaceinfoPlayer + 0x2C` is not the race clock. It starts at the intro camera and every event in the log was stamped 6.87s later than it happened, which is why a lap time of 0:31.249 was printed at 0:38.100.
- `2026-08-12`: **race clock SOLVED -> `Raceinfo + 0xA98`.** Found by scanning the Raceinfo object for words that hold still through the countdown and move afterwards, then keeping the one that counts at 60 Hz. `analysis/validate_race_clock.py` on all seven recordings: exactly 0 for every frame of the intro and countdown; first non-zero frame never after the first frame any racer moves; `+0x2C - 0xA98 = 412` on 18,437 of 18,441 frames where both advance (the four exceptions are 408-411, a snapshot between the game's two writes, not a drift); and it agrees with the game's own cumulative lap `Timer` - a different pointer path entirely - at every lap boundary, worst 0.18s against a 20 Hz sampling interval.
- `2026-08-12`: the useful half of that is which one survives. Where the two disagree it is always `0xA98` still counting and `+0x2C` stopped; the reverse happens on 0 of 21,926 frames. `+0x2C` is that racer's race ending, not the race ending.
- `2026-08-12`: CORRECTION -> `+0x2C` "freezes when that racer finishes" is too narrow. In `yoshi-falls` and `mushroom-gorge` the human never crossed the line and it stopped anyway, at the frame the last CPU finished. Stored per racer as `raced` in a race log, which is what stops a DNF racer being credited with the position they were parked in after the race ended.
- `2026-08-12`: CORRECTION -> `+0x26` is NOT "laps in this race". It is the highest lap that racer has reached: it reads 1, then 2, then 3, following `+0x24` up and stopping where they stopped. It looks like a lap count only in a finished race, which is why the live view was showing "lap 1/1" and "lap 2/2". Renamed `OFF_MAX_LAP` -> `OFF_LAP_REACHED`. The race's length is not read anywhere yet; `mkw/racelog.py` takes the maximum over the field, which is right whenever somebody finished.
- `2026-08-12`: CORRECTION -> `+0x30` (frames in first) also counts from the intro, so whoever is on pole is credited about 6.87s of countdown as time in first. Caught by rebuilding the same quantity from position changes and finding exactly one racer per race out by 6.847-6.920s, always the one with grid 1. Subtracting `COUNTDOWN_FRAMES` makes the two agree for all twelve racers in all seven recordings. The reader keeps the raw value; `mkw/report.py` corrects it.
- `2026-08-12`: **race logs.** A recording is ~1 GB and only exists to find things; there is no reason to keep one to know what happened in a race. `mkw/racelog.py` writes the event stream as JSON Lines - header, events, final standings - and `mkw/report.py` rebuilds the whole report from that file with no memory and no recording. Ids are stored rather than names, so correcting `mkw/names.py` corrects every stored race.
- `2026-08-12`: `mkw/events.py` now emits dicts rather than formatted strings, and `describe()` renders them. That is what makes the stored file and the live view provably the same thing rather than two renderings that drift.
- `2026-08-12`: position changes are now events. They were not tracked at all before. 585 per race on average, about two thirds of a file, most of them the scramble off the grid - kept, and hidden from the printed stream, because time-in-position cannot be rebuilt without them.
- `2026-08-12`: `analysis/validate_race_log.py` -> **7 of 7 recordings pass both checks.** Round trip: replay the recording, write the log, read it back and render every event from the file - 6,044 events, every line identical to the live one. Independent check: time in first place rebuilt from the stored position changes matches `+0x30`, which the rebuild never sees, for all twelve racers in every race, within one 20 Hz sample per crossing into or out of first.
- `2026-08-12`: **a race costs 34-71 kB, 54 kB on average** - 62-66 bytes per event, 0.24-0.51 kB per second of racing. About 20,000 races per gigabyte, against roughly one race per gigabyte for a recording. Storage is not a constraint here and is not worth optimising.
- `2026-08-12`: **sessions.** A race log per race was the wrong unit on its own - James plays a VS sequence and wants the three or four races together. `tools/track.py` is now the one command: start it, play, ctrl-c, and every race in between is a file in one session directory with an index rewritten after each. Nothing about that needs a memory read - a session is however many races were played between starting the tool and stopping it - which is why it works today rather than after the VS race index is found.
- `2026-08-12`: races are separated by the race clock going back to zero, not by the course changing. Two races on the same course would otherwise have been one file. Three consecutive frames of it are required for the same reason `DROPOUT` is 20: a live snapshot can tear, and the countdown lasts 137 frames at 20 Hz so there is no hurry.
- `2026-08-12`: `tools/live.py` deleted; `tools/track.py` renders the same view and stores what it sees. Two commands that differed only in whether they threw the race away was not worth the duplication.
- `2026-08-12`: **progress is now stored, 5 times a second.** Events say what happened; this says where everyone was while it happened, which is what a gap or a chase is made of, and it is what makes a race replayable rather than just readable. 12 racers x 5 Hz x 3 minutes is about 65 kB, which takes a race from 54 kB to 119 kB - 8,800 races per gigabyte against roughly one for a recording.
- `2026-08-12`: first attempt sampled whatever value the current frame happened to hold, which is up to a frame late - a quarter of a 5 Hz step. Interpolating each racer to the exact grid time from the frames either side took the median error against the 20 Hz reads from 0.00070 laps to **0.00003**, and the share within 0.01 of a lap to 99.8-100%.
- `2026-08-12`: **CORRECTION -> race completion `+0x0C` is not monotonic.** Crossing the line on the final lap puts it back to the start of that lap: 3.9994 then 3.0002 on GCN Peach Beach. Interpolating through that step produced values a whole lap out, which is what the worst-case error in `analysis/validate_session.py` was measuring before the step was recognised for what it is. Both the sampler and the reader now treat a jump of more than half a lap as a discontinuity.
- `2026-08-12`: `RaceConfig + 0x28 + 12*0xF0` (= `cfg + 0xB68`) is the race's settings block - `settings[0]` is the course id and agrees with the validated `COURSE_PTR` path 7/7. The rest is NOT decoded and is deliberately not guessed at: every recording is a single race, so nothing in the block can be told from a constant. All sixteen words are now stored raw in every race log so a VS sequence recorded later answers it.
- `2026-08-12`: `analysis/validate_session.py` -> **all checks pass.** Seven recordings fed back to back through one `Race` and one session `Recorder` come out as seven separate races, every one with the same event count the live run produced, and progress reproducing the full-rate reads as above.
- `2026-08-12`: session points use the published MKW VS table (15/12/10/8/7/6/5/4/3/2/1/0) applied to the finishing positions, not a total read from memory - that has not been found. `Session.same_field` checks the same twelve racers are in the same slots in every race first, because adding up unrelated races by slot number would otherwise look like a series.
- `2026-08-12`: **bug, reported by James -> the tracker died at the start of his second race.** `TypeError: %x format: an integer is required, not NoneType` in the live view's header. `read_course` returns None while the course pointer is being rebuilt between races, which is exactly the moment race two begins. Race one was already safely on disk; the rest of the session went unrecorded. Fixed three ways rather than one: `mkw/names.py` gains `course_name()` and every caller that was formatting the code by hand now uses it, the view falls back to the course the race already knows, and the whole display is wrapped so a drawing bug can never again take down a session that is recording correctly.
- `2026-08-12`: **second bug, found by the test written for the first.** The clock-went-backwards check compared against the previous frame's clock, which had already dropped to zero, so the run of backwards frames only ever reached 1 and `RESTART = 3` was never met. Two races on the same course would have been written as one file - the exact thing that check exists to prevent. Now compared against the highest clock the race has reached. `analysis/validate_transitions.py` covers both: every snapshot field blanked in turn, and a race, an unreadable gap, then a second race on the same course -> 2 races stored.
- `2026-08-12`: **first live session end to end.** Three VS races recorded from Dolphin through `tools/track.py`, read back with `tools/report.py` alone: Luigi Circuit, DS Yoshi Falls, SNES Ghost Valley 2, P1 in all three, 1,728 events, **249 kB for the three - 78, 79 and 86 kB**, against roughly a gigabyte per race for a recording. 17 of the 18 hits involving slot 0 were credited to a thrower; the one that was not had two candidates in the despawn window and is logged unknown rather than guessed.
- `2026-08-12`: OPEN, and the next thing worth doing -> a triple counts as one `use`. Across those three races items held and items used were equal item for item, 21 and 21, which cannot be right when four of them were Triple Bananas. A throw needs to be an event per object spawned, not per held-item field going empty, or the items UI will undercount every triple.
- `2026-08-13`: audit of the event stream against the five stored races, before writing any code. Findings, each with how it was counted: **`swap` has never fired** (0 in 5 races); **a triple's `use` is the deploy, not a throw** (hold->use median 0.05s, n=10 green and n=41 banana, i.e. one sample); **Golden Mushroom is one `use` for every boost it gave** (median 9.07s after pickup, n=28); **109 hits carry `by: null`** (all of damage 3, 6, 10, 11, 13, 17) against 152 named; **4 hits are a racer driving into their own item**; 376 `box` against 355 `use`, so about 20 items a session are picked up and never fired.
- `2026-08-13`: CLOSED BY DECISION, James -> do not chase throws inside a triple, or per-boost Golden Mushrooms. What the log is for is when you got them and when they deployed, and it already says that. This is a decision, not a failure: the method is known (watch each object's state change in the pools) and is deliberately not being spent on. Supersedes the 2026-08-12 "OPEN, and the next thing worth doing" line above, which also proposed the wrong fix - the pool goes 0 -> 3 at deploy, so an event per object spawned would give three throws at the moment nothing was thrown.
- `2026-08-13`: **an item leaving the held field is not always a throw.** A Lightning read as eleven racers all choosing to use what they were holding in the same frame. `analysis/validate_item_loss.py` walks all 442 clearings across the seven recordings: 415 have no hit within 0.6s and are plain throws, 25 happen while that racer's damage field is reading a type that flips, flattens or shocks them, and 2 have a hit near but not on them. Spin-out and knockback never take it; damage 3, 6, 7, 8, 10, 13 and 17 do. Items taken: 9 Golden Mushrooms, 4 Red Shells, 4 Stars, 3 Fake Item Boxes, 2 Triple Mushrooms, a Banana, a Mega Mushroom, a Bullet Bill.
- `2026-08-13`: a time window was tried first and is worse, worth writing down. Pairing a clearing with a hit 0.20s before it gives 23 of the 25, because it depends on the reader catching the frame the hit began and a partly unreadable snapshot moves that by up to 0.65s. Asking what the damage field says **at the moment the item goes** needs no window and no constant, and reader and offline check then agree exactly: 417 `use` and 25 `lost` from both.
- `2026-08-13`: `lost` is its own event with the damage type that took it, rather than a flag on `use`, and stays out of `Race.uses` so a dropped item is never credited as the throw that spawned a nearby object. `analysis/validate_race_log.py` and `validate_session.py` still pass 7 of 7.
- `2026-08-13`: **hits with no object behind them now get a name.** A Star, Mega or Bullet does its damage with the kart itself and Lightning and the POW have nothing in the world at all, so `by` was null for every damage 3, 6, 10, 11 and 13. `lab/events/measure.py` measured what is left to go on: a ram's victim and the racer who did it are 0.0001-0.0030 laps apart and the item was used 0.6-9.1s earlier, and requiring both gives exactly one candidate for 32 of 37 Star rams, 17 of 17 Bullet rams and 22 of 22 Mega crushes. Lightning lands in the same frame it is used (lag 0.000s over 27 hits) and a POW 1.95s later, and the user is never among the victims.
- `2026-08-13`: `analysis/validate_attribution.py` -> **312 of 365 hits across the seven recordings now carry somebody's name, against 176 before.** The 53 without are 37 track hazards (fire, Pokeys, Cataquacks), 8 Thunder Cloud strikes which carry `from` instead, and 8 whose object was missed. Checked against things the rules do not look at: the racer credited with a ram was not taking damage themselves, which is what riding a Star, Mega or Bullet means, 74 of 74; and the racer credited with a Lightning or POW is not among its victims, 62 of 62.
- `2026-08-13`: first version of that check asked whether the racer credited with a ram was close to the victim, which is the rule agreeing with itself. Worth writing down: a check built out of the rule's own condition passes 74 of 74 and means nothing.
- `2026-08-13`: **Lightning needed the hit held back, not a wider window.** The item field clears one sample AFTER the damage lands, so at the moment the strike is seen the use that caused it has not been read yet - ten of eleven victims went out unattributed. Hits of a field-wide type now wait 1.0s before being reported, the same mechanism the object-named hits already used. `HOLD` split out from `LAG` because the two are the same number for different reasons.
- `2026-08-13`: **a hit now has a length.** `read_damage` returns to -1 when the racer recovers and that transition was read every frame and thrown away. Measured spans: banana 0.66s median, shell 1.70s, Mega crush 0.35s, Bullet 2.35s, longest 4.90s. Reported as `for` on the hit, which makes time-out-of-the-race a number rather than a feeling.
- `2026-08-13`: **direct hit vs caught in the blast.** A Blue Shell aims at whoever is leading; a Bob-omb aims at nobody. Across the seven recordings 14 explosions caught one racer, 3 caught two and 1 caught three, and of the four multi-victim ones two contained the leader and two did not - so "the first racer hit was the target" is wrong half the time and is not the rule. The rule is the game's: in a multi-victim blast the leader is the hit if the object was a Blue Shell, everyone else was caught in it. Hits carry `place` now, so this is decidable offline too.
- `2026-08-13`: **Thunder Cloud events.** The cloud never appears in the held item field - it goes to work the instant it is won - which is why it looked invisible. It is a world object of type 14, live for 11.1-11.2s, and its owner is who won it. A `cloud` event on the object appearing plus the existing damage-17 hit gives who won it and who it went off on: 9 clouds, 4 kept, 4 passed on before it struck. Who passed it to whom is not recorded because it is not readable - see Open.
- `2026-08-13`: **events after a racer's own finish are marked, not counted.** They were being counted like any other, so a shell catching somebody parked past the line was in their totals. 3 such hits in the five stored races and 4 across the recordings - small, but the whole point is that the numbers are arguable with nobody. Kept in the log with `after: true` because it did happen, and `mkw/report.py` builds every total from the rest.
- `2026-08-13`: lap events are stamped with the game's own cumulative lap timer now, the way `finish` already was, rather than with the clock on the frame the crossing was noticed - which is up to one 20 Hz sample late.
- `2026-08-13`: the local player is read from RaceConfig (`type == 0`) instead of assuming slot 0. No change on any recording - all seven have exactly one non-CPU and it is always slot 0 - which is the point: it is now a read rather than a fact about how James plays. Two humans on one couch are both type 0 and the first is "you".
- `2026-08-13`: CORRECTION, same day -> the pause entry first said the race clock stands still because Dolphin runs below full speed under the recorder. It does not: the recordings run at 20.7-23.6 frames per game-second against a 20 Hz recorder, which is real time. The measurement that killed the check is unchanged, the reason given for it was wrong, and the cause is stalls rather than a uniform slowdown. Worth retesting live, where nothing is being written to disk.
- `2026-08-13`: `analysis/validate_live_view.py` -> the live view is drawn for every frame of every recording, 21,926 of them, with 0 raising and the local player named on all of them. Added because the two sessions James has lost were both lost to the drawing rather than to the reading, and the wrapper that stops that taking a session down also hides a view that shows nothing.
- `2026-08-13`: `tools/track.py` still had `A.LOCAL_SLOT` in three places after the reader started deriving it. Same value on this setup, so nothing was visibly wrong, which is what makes it worth writing down.
- `2026-08-14`: first live session with two Wii remotes (`races/20260814-122832-two-remotes`) -> both local players read as `type == 0` in RaceConfig (slots 0 and 1, Birdo and Luigi), 10 CPUs as `type == 1`. One race from one launch, so the multi-human claim now has evidence but not the two-launch standard; a second session on another day would settle it. The dashboard's fallback tracked-player list (the humans found in the races) picked both up unaided.
- `2026-08-17`: the dashboard was rebuilt in TypeScript, React and Tailwind (`web/`), and `mkw/stats.py` plus `tools/dashboard.py` were deleted rather than kept alongside it. James asked for the change; the reason to delete rather than keep both is that the same rules in two languages means a correction landing in one of them. `mkw/names.py` stays the single source of the id-to-name tables and `tools/export_names.py` generates the TypeScript copy from it, so that split cannot happen quietly. Everything the dashboard shows is still derived from the stored logs by fixed rules, now in `web/src/lib/stats.ts`.
- `2026-08-17`: **course outlines exist for all 32, traced from layout drawings.** The wiki has a top-down line drawing per course (`tools/fetch_tracks.py`); the road is the region its outline encloses, so `tools/build_tracks.py` thins that region to one pixel wide and takes the longest route through what is left. Self-check per course is `covers` = traced lap / all road found in the drawing: 31 of 32 land in 0.76-1.11, and DS Peach Gardens reads 0.49 because the drawing includes the hedge maze, which is not road. Nine courses are drawn with gaps in the road (Rainbow Road, Grumble Volcano, Mushroom Gorge, Koopa Cape and others) and their pieces are joined end to end; the largest real gap is a third of the drawing's diagonal, which is what set the joining limit.
- `2026-08-17`: three things that went wrong in that tracing, because each looked like a different bug than it was. An 8-connected skeleton counts every staircase pixel as a junction, so a clean loop came back as 115 junctions and the loop search failed - the fix is dropping a diagonal edge when the same two pixels are already joined by two straight steps. Pruning stubs against the *total* skeleton length ate whole laps on courses with few branches; against the longest chain it does not. And `stitch` started from `list(max(pieces))`, so `p is not chain` was true of the piece it had just copied and every single-piece course got traced twice round - which showed up only as lengths that were suspiciously close to double.
- `2026-08-17`: **the game's own course geometry is a better answer and now has a reader.** Each course's `course.kmp` holds checkpoints: pairs of points across the road, in order round the lap, starting at the finish line - so real shape, real start line, real direction, none of which a drawing knows. `tools/kmp.py` reads it and `tools/build_tracks.py --kmp <dir>` rebuilds the outlines from it. Verified against the only three vanilla course files published anywhere fetchable (`ThomasAlban/kmpeek` test fixtures): their checkpoint laps sit 6.1%, 7.7% and 3.2% from the traced Coconut Mall, Dry Dry Ruins and DK Summit drawings, which both confirms the reader and confirms three entries of the course-id-to-folder-name table. On DK Summit the checkpoints are visibly the better of the two - the traced line cuts a corner the real road does not.
- `2026-08-17`: OPEN, and needs the disc -> the remaining 29 course files are not published anywhere (the physics reimplementations deliberately ship no assets, everything else is `.szs`), so exact outlines need one extraction from James's own ISO: `wszst extract RMCP01.iso --dest ./courses --files '+/Race/Course/*.szs'` then `python3 -m tools.build_tracks --kmp ./courses`. No Dolphin, no sudo, a couple of minutes. Until then the replay measures a lap from where the tracing began rather than from the start line, and says so on screen.
- `2026-08-17`: **the course drawings are no longer drawn.** The replay was putting the source PNG on screen, and those are 101x275 to 230x267 - the whole point of them - so at replay size the course was a soft grey smear. `tools/build_tracks.py` now emits the road as a *path*: every side of a road pixel facing something outside the road is a unit edge, the edges chain into closed loops, and the loops are filled even-odd so a course drawn as a ring keeps its hole. Only the pieces the lap runs through are asked for their edges, so decoration the trace already discarded - the mushrooms in Mushroom Gorge, the hedges in DS Peach Gardens - does not come back as road. 32 of 32 build; 1 to 23 loops each, and the 23 is Peach Gardens' maze, which is the same false positive `covers` already flags.
- `2026-08-17`: two things worth writing down from that. Rounding the staircase off puts a point every pixel, which came to 606 kB of `tracks.ts`; Douglas-Peucker at 0.25px cuts it to 228 kB and changes nothing you can see. And the first version of that ran DP on the loop with its first point repeated at the end - every point is then zero distance from the line between two identical points, so it kept two of them and threw away all 32 outlines. Splitting the loop at its farthest point and simplifying the two halves fixes it. Symptom was `outline: "M 2.0 206.0 Z"`, which is a shape that cannot be seen rather than one that looks wrong.
- `2026-08-17`: the replay used to draw all twelve karts on the exact point their lap fraction gives, which at the start line is twelve karts in one place. They are now offset sideways into three lanes by running order. That offset is not in the log and is not claimed to be: the caption says how far round the lap a kart is comes from the log and which lane it sits in does not.
- `2026-08-17`: **the tracker's own files could not be written to.** Naming the players from the dashboard writes `players.json` into the session directory, and four of the five stored sessions belong to `root` - `tools/track.py` needs sudo to read Dolphin's memory, so everything it writes lands owned by root. `mkw/session.py` now chowns the directory and every file it writes back to `SUDO_UID`/`SUDO_GID`; sessions recorded before this need one `sudo chown -R "$USER" races/<session>`, which the dashboard says when a save fails. Found by the save killing the dev server: the write threw EACCES inside a promise, which in Node is an unhandled rejection and takes the process down. Both fixed.
- `2026-08-17`: **a lap started and ran wherever the tracing happened to.** The centreline is right about the road and says nothing about where the start line is or which way round the course is driven, so a replay put the pack on the right tarmac going a plausible wrong way from the wrong place - which reads as the tool not knowing the route. Both facts are now set by hand, once per course, at `#/tracks`, and kept in `assets/tracks/starts.json` as a point on the drawing plus a reverse flag. A point rather than a distance along the path, so re-running `build_tracks.py` does not move it.
- `2026-08-17`: finding the start line automatically was tried first and does not work. Some layout drawings mark it with a grey band across the road - Mario Circuit has two, GCN Waluigi Stadium has two - so grey pixels enclosed by the outline were clustered across all 32. Most of what comes back is the anti-aliased edge of the outline itself (Luigi Circuit's largest grey component is 1px wide and 52px long), and courses that do mark it are the minority. Direction is not in the drawing in any form. This is a decision to ask rather than to detect: the method is known and is not worth the false positives.
- `2026-09-03`: **James picked a Star up off the road and the log has no `hold` for it.** `races/20260903-194727-amy-2-player/01-mario-circuit`: slot 4 lost a Star at 33.267 (damage 3), and slot 0's next item event is a `box` at 42.783 whose roulette landed on Triple Mushrooms. Counted across all 13 stored races: 715 `hold` events, 715 with a `box` for the same racer and item before it. Box to hold is 1.0-1.4s for a CPU and 3.5-3.8s for a human, except 5 human holds at 1.20-1.65s (all in the 2026-08-12 versus-night races, which have no recording) - unexplained, and noted so they are not later mistaken for anything without checking.
- `2026-09-03`: read the code before the recordings. `hold` is emitted on the held field alone - `+0x08F` going 20 -> item, `mkw/events.py` - with no condition on the roulette, so a pickup off the road that writes that byte would already be logged. The one way a hold can hide is `read_items` failing closed: any byte above 20 in either field for any racer drops the whole frame. Counted below.
- `2026-09-03`: `analysis/floor_pickups.py` -> **no pickup off the road exists in the three recordings.** 216 held-field 20 -> item transitions (34, 109, 73; the same 216 `hold` events `tools.replay_live` emits), and every one has that racer's roulette field non-empty in the 6s before it. 13 losses (held cleared while the damage field read a DROPS_ITEM type), and nobody's held field filled without a roulette in the 20s after any of them. Item fields were unreadable on 0 of the 8,875 in-race frames, so nothing was hidden by the fail-closed read either. So the question is undecidable offline; see Open.
- `2026-09-03`: **a dropped item is a world object.** All 13 losses spawn an object in the pools, of the dropped item's type, with `+0x6C` owner = the racer who lost it, 0-0.1s before their held field clears (the held field clears one sample after the damage lands, 2026-08-13). Object types learned from what was dropped: Red Shell -> 1, Banana -> 2, Triple Mushrooms -> 3 (2 recordings, separate launches), Star -> 4 (2), Fake Item Box -> 7, Mega Mushroom -> 8 (1), Golden Mushroom -> 12 (4), Bullet Bill -> 13 (1). This is what the "1 and 3 spawns" of types 4 and 12 in MEMORY_MAP were. CORRECTION to the Open bullet that guessed type 3 was a held or dragged shell: it spawns when Triple Mushrooms are dropped, so it is a Mushroom. 8 and 13 are one recording each and stay out of `mkw/names.py` for now.
- `2026-09-03`: how long a dropped item lasts, from object born to gone: Golden Mushroom 0.45/0.67/0.70/0.87s, Mushroom 0.53/2.03s, Mega Mushroom 0.75s, Bullet Bill 0.20s - gone before anyone could drive over them. Red Shell 3.77s, Fake Item Box 11.0s and one still there at the end, Banana 63s: those become hazards. Star 30.2s, and one still there 26.5s later when the recording ends. A Star is the only dropped pickup that lasts, so it is the one to test with.
- `2026-09-03`: one candidate looked at frame by frame and rejected. Waluigi Stadium: Dry Bones (slot 2) dropped a Star at 93.4 (Bullet Bill); the object went at 123.55, 0.1s after slot 2's own roulette started with Star as its result, held 1.15s later. But slot 2 was at completion 3.016 - just past the line, where the first boxes are - against 2.494 where it dropped it: half a lap away, so an ordinary box that rolled a Star, and the Star's 30.2s is a timeout. Yoshi Falls: slot 4 got a Golden Mushroom 0.05s before a dropped one vanished, but its roulette had started 0.5s before the drop. `lab/items/dump_item_window.py` is the frame-by-frame view.
- `2026-09-03`: OPEN -> what the Mario Circuit log can and cannot say. The Star slot 4 dropped at 33.267 would have been on the road until about 63s. Slot 0's only field activity in that window is the box at 42.783 that rolled Triple Mushrooms, 3.55s before the hold - an ordinary roulette by every measure available. Either the pickup was not written to `+0x08F`/`+0x077` at all, or it runs a roulette that the log cannot tell from a box. Needs a recording where a human drives over a dropped Star on purpose; the type-4 object leaving the pool in the same sample is the anchor to look at either way.
- `2026-09-03`: **the dashboard is a grid of widgets that expand, and a race has its own screen.** James's direction: the charts were full-width and "massive", he wants small widgets beside each other like a data dashboard, and a way to drill down. The night is now the leaderboard, then a grid - points, blue shells, awards, items, hit by, who hit who, totals - each opening large; a race row opens `#/race/<session>/<race>`, with the per-player table and the same grid for that race alone (how it unfolded, time in each position, items, every hit and who threw it, every blue shell, who hit whom) and the replay a click away. Everything is still computed in `web/src/lib/stats.ts` from the logs. The blue shell widget now follows the slider like everything else; it was the one thing on the screen that did not.
- `2026-09-03`: **new derived numbers, no new reads**: items picked up, thrown and knocked out of hand, per item (`hold`, `use`, `lost`); hits taken by cause (the object read, else the damage type's description); who hit whom as a matrix over the players and every CPU character, single-owner hits only, the rule the nemesis line already used; time in each position per race from the `pos` events. Verified against the amy-2-player session: Birdo's 25 got / 22 thrown / 23 landed / 14 taken are the sums of the per-race rows the old table showed.
- `2026-09-03`: **blue shell dodges are derived.** James was blued in the cannon on Maple Treeway and it never landed; the log has the `use` at 114.2s and no launched hit on him after it. So a dodge is a Blue Shell `use` with no launched, un-`caught`, blue-object hit within 15s, credited to whoever was leading among the unfinished racers at the throw (`stats.ts::blueShells`). Measured over the 13 stored races: 30 thrown, 25 landed, 5 dodged; use-to-hit gap 3.2-9.6s, median 5.2s, which set the 15s window. The hit is looked for on the leader first and then on anybody: 1 of the 30 (Maple Treeway, 181.5s) landed on the racer who had been P4 at the throw and crossed the line while it was in the air, so the leader at the throw is the target nearly always, not always. Two logs from before `caught` existed have a second, unflagged hit per shell; those stay unmatched and change nothing.
- `2026-09-03`: reported by James -> the Sniper award read "23 of 22 landed". Not a bug: a triple is one `use` (the deploy) and each banana that lands is a hit. The label now says "23 hits from 22 throws · a triple is one throw". Decision: still not counting a triple's three shots, for the reason in RACE_LOG.md.
- `2026-09-03`: reported by James -> "back to the night" is now "back to stats" everywhere, and the replay's "What happened" list, which showed the last nine lines and stopped growing, now holds every line so far and scrolls.
- `2026-09-03`: the new dashboard numbers were checked against `tools/report.py`, which is the Python side of the same measurements and shares no code with them. Maple Treeway, race 4: Birdo 7 items in hand, 5 thrown, 7 landed, 4 taken, 6.5s out, hit by 2x Blue Shell + 1x Lightning + 1x Green Shell, P1 125.3s / P2 32.5s / P3 17.4s - every figure the same on both sides, including the position split the new widget draws as a bar. The who-hit-who column agrees too: Birdo's 4 hits taken are 1 of his own, 1 from Toad and 2 from CPUs, against `report.py`'s "1x Yoshi, 1x Toad, 1x Wario" plus the self-hit. One deliberate difference: the dashboard's "got" counts `hold` (the item actually in your hands) where `report.py` counts `boxes` (item boxes driven through). They are equal in all 13 stored races, and they would not be if a roulette were still spinning when a race ended, or if an item were ever picked up off the road.
- `2026-09-03`: **the traced centrelines went round islands and back.** James saw Daisy Circuit's start jump about and the line go round in a circle. Cause, confirmed by drawing the lap over each drawing (`tools/build_tracks.py --check DIR`, a PNG per course at 4x with the first point and the 10% marks): `longest_loop` forbade reusing a chain but not a junction, so wherever the road forks round an island it went round the island and carried on. 14 of 32 courses had a ring, a double-back or a cut across the drawing; Daisy Circuit came out open with its courtyard stitched on as road. `covers` had said 0.93 for it, which is why a number is not a check here.
- `2026-09-03`: shortest paths for open pieces were tried first (longest of the shortest paths between two dead ends) and are wrong: Maple Treeway's cannon is drawn as road from the bottom of the treetops to the top, so the shortest way is up the cannon and the treetops are skipped (`covers` 0.54); GCN DK Mountain cut through where its folds touch (0.64). Open pieces use the longest route that passes no junction twice instead (depth-first over the chains, 200k-step budget). Maple 0.87, DK Mountain 0.80.
- `2026-09-03`: closed courses use the *shortest* loop that goes once round the interior, found by cutting the drawing along a ray from the interior and taking the shortest skeleton path between the two sides of one crossing. Longest was not tried, by reasoning rather than measurement: it would zigzag through DS Peach Gardens' hedge maze and round Moo Moo Meadows' mole holes, and the drawings show it. Shortest goes straight through the maze and takes one side of every fork. 21 of 32 courses now come out closed, against 12 before; Daisy Circuit, Dry Dry Ruins, N64 Sherbet Land, Delfino Square, Yoshi Falls, Peach Gardens, Moo Moo Meadows, Toad's Factory and Waluigi Stadium were all open, and an open path the app rotates to the start line puts a jump mid-lap.
- `2026-09-03`: the first version of that took the union of everything a piece encircles as the interior, and on Peach Gardens the nearest pixel to the centroid landed in a hedge: a 103px loop round one hedge, `covers` 0.07. The interior is the largest thing encircled, and only if it is at least 30% of the piece's own area, so a ring round a roundabout island does not count.
- `2026-09-03`: a loop that passes no junction twice loses GCN Peach Beach's out-and-back (`covers` 0.64): the road leaves the ring at one pixel, goes round two islands and comes back to the same pixel. Whatever the lap left out that hangs off it at exactly one pixel and contains a loop is spliced in - out along the stem, round the longest way, back along the stem. 0.80, and the drawing shows the hairpin. A first attempt searched for the loop *from* the attach pixel and found nothing, because the junction at the top of the stem is passed twice.
- `2026-09-03`: stitching pieces by nearest end sent Grumble Volcano, Rainbow Road and Koopa Cape straight across the drawing. Held-Karp over piece order and direction minimising total gap (open chain) still cut across on Grumble, because the open chain drops whichever gap is largest and the course is a ring. The tour is now a round trip cut at its widest gap, and a piece with more than two dead ends offers its longest route between each pair, with the road it skips counted as gap. Grumble is a ring; Rainbow Road 1.11 (doubled) -> 0.99.
- `2026-09-03`: piece filters, each tried against all 32: `MIN_PIECE` 0.15 -> 0.05 (GCN Waluigi Stadium's middle straight is 14% of the lap and N64 Bowser's Castle's corkscrew 9%, both were dropped). A closed longest piece keeps nothing else (the courtyards). A piece is a blob, not road, when its area is more than 1.6 times its lap length times its width (Koopa Cape's river triangle 1.7, road 1.0-1.45), unless it is a ring round something, which is measured along the ring (GBA Shy Guy Beach's ring pieces were lost to this). Width is twice the median distance from the lap to the edge of the piece, because area over skeleton length reads a blob as narrow. A rule dropping the narrower of two pieces sharing both ends was tried and removed: it dropped Koopa Cape's hairpin by the start, which is driven.
- `2026-09-03`: "inside what the main piece encircles" is measured with the piece grown 3px so the flood cannot slip along its own ink, and that closes the breaks drawn at bridges - GCN Waluigi Stadium's middle straight then read as inside. A piece whose end meets an end of the main piece is exempt. The parallel-route rule (both ends on the middle of another piece) also needs the stretch between those ends to be about the piece's own length, or the same straight is dropped as the other way round an obstacle. Waluigi 0.98 open -> 1.05 loop through the straight.
- `2026-09-03`: **direction is preserved across rebuilds.** The reverse flag in `starts.json` means "against the path" and only survives if the new path runs the same way round. The tool now reads `tracks.ts` before writing and turns each re-traced path to match, judged by walking the new path and asking whether each point's nearest old point comes later along the old path than the last one's did (signed area was not used: a figure-eight's two lobes nearly cancel). 32 of 32 match the paths James set his flags against; on Grumble Volcano (30 forward, 27 back), GBA Shy Guy Beach (29/22), Rainbow Road (38/23), Koopa Cape (45/34) and Maple Treeway (45/26) the old path was wrong enough that the vote is close, and the flag on those wants a look at #/tracks.
- `2026-09-03`: `build_tracks.py luigi` did not write a partial `tracks.ts`; it wrote nothing. It now rewrites the matching courses and carries the rest over from the file, checked by running it and diffing (identical). Rebuilding twice gives an identical file.
- `2026-09-03`: OPEN -> what is still wrong after this, from the check images. GBA Shy Guy Beach: the beach is drawn with rocks touching both its edges, which cut it into pieces, and the top-left corner piece thins to 37px wide and fails the width limit; the lap goes round once with three jumps and cuts the top-left corner (0.80). Grumble Volcano: the longest route through the pillar section wiggles round three drawn pillars, and the top-right arm of the lava loop is skipped (0.79). N64 Bowser's Castle: the lap leaves the bottom-left room through its wall to reach the corkscrew. Rainbow Road's piece order is the one with the shortest gaps and is not checked against the real course. DS Yoshi Falls 0.64 and DS Peach Gardens 0.46 are the other lanes and the maze counted as road, and the laps look right. The game's own checkpoints (2026-08-17, needs the disc) remain the real answer to all of it.
- `2026-09-04`: **the lap can be drawn by hand**, at `#/tracks` under Draw, saved to `assets/tracks/routes.json` and used in preference to the traced centreline. James's call, after seeing the retraced courses: the tracing is worth having and he would rather draw the ones it gets wrong than have the tracer chased any further. A drawn lap carries its own start and direction - first point, then the order - so it needs no entry in `starts.json`, and the replay measures along it by arc length, so the points need not be evenly spaced. Verified end to end in the browser: four clicks on Mario Circuit save as four points in the drawing's coordinates, the replay then drives on that path and says on screen that the lap was drawn by hand, and Remove puts the course back on its traced lap. The request body limit in `web/api.mjs` went from 64 kB to 1 MB for it, and a route is capped at 500 points.
- `2026-09-04`: a decision, not a failure -> the remaining bad traces (GBA Shy Guy Beach and the two courses `covers` still flags) are not being chased in code. The method is written up in the line above this one and in the tracing entries for 2026-09-03; drawing one course takes about a minute and does not have to be right for all 32 at once.
- `2026-09-04`: **the leaderboard can show the CPUs**, off on every page load. James asked to be able to toggle them on. A CPU is added up by exactly the code a player is - `stats.ts::raceRows` now builds a row for every slot and `totalsOf` adds up whatever rows it is handed - and the twelve go on one ladder by points, players in front on a tie. Identity is the character, the key `duelTotals` and `hitMatrix` already use, so the Waluigi on the leaderboard is the Waluigi in the matrix; two CPUs on one character would be one row, and no stored race has a repeated character (checked: 13 races, 12 distinct each). Cross-check on amy-2-player: the twelve cards total 292 points, which is 4 races x the 73 points VS pays out.
- `2026-09-04`: **items and hit-by columns are in one fixed order** (`stats.ts::ITEM_ORDER`, `causeRank`), front of the field to back: banana, triple bananas, fake item box, green, triple green, red, triple red, bob-omb, mushroom, triple mushrooms, blooper, POW, thunder cloud, mega, star, golden, bullet, blue, lightning. James gave the sequence; the ones he did not name sit with their neighbours. Hits keep the same order and then two tails - the damage types read without an object, which name two items ("Shell or Fake Item Box"), then everything nobody threw (an enemy, Chain Chomp, a boosted kart, the Moonview traffic, a Cataquack, a Thwomp, a Zapper, fire). It was by count before, so the columns moved as the races landed. Held by name rather than by id, so a corrected id in `mkw/names.py` carries the order with it. The narrow strips still pick which items to show by count - six will not fit nineteen - and draw those six in this order.
- `2026-09-04`: reported by James -> an item knocked out of somebody's hands is now a red ring round the cell rather than a "−2" beside the got/thrown pair, which read as arithmetic on them. The count moved into the tooltip. Same numbers, `itemTally.lost`.
- `2026-09-04`: **a Scavenger widget**, for items picked up off the road. The rule, since nothing else in the log tells the two apart: a `hold` with no `box` for that racer in the 6s before it, each box counting for one hold. Measured over the 13 stored races: 715 holds, box-to-hold 0.98-3.80s (median 1.15), and **0 scavenged** - which is the same count the 2026-09-03 entries got and means "the recorder saw none", not "there were none". The widget says so on its face rather than showing an empty row per player, and links the open question. James picked a Star up off the floor on Mario Circuit and this is where it will appear when a recording finally has one.
- `2026-09-04`: the dashboard grid is now points progress two wide and two tall, blue shells and awards stacked beside it, then items / hit by / scavenger, then who-hit-who and totals full width. James's layout. The race screen keeps the same shape with the blue shell list two wide beside the scavenger. The word "night" is gone from the screen and the comments - a session is a session whatever time of day it is played.
- `2026-09-04`: **a pickup off the road looks exactly like an item box, and where round the lap it happened is what gives it away.** Item boxes sit in rows at fixed points on a course, so an ordinary pickup always has another one near it round the lap. Took every `box` event in the stored races, put each at its own racer's lap fraction from the `progress` record, and measured the distance to the nearest other box in that race. 771 boxes with progress under them; 769 have another within 0.012 of a lap. Two do not: Wario's Gold Mine 2026-08-12 at 0.054, and **SNES Ghost Valley 2 2026-08-14 race 2, slot 0 at 56.483s, 0.9414 round lap 3, Triple Red Shells, 0.177 from the nearest box in a race with 58 of them**. Ghost Valley's three rows are at 0.12, 0.37-0.42 and 0.70-0.72 in all three of its stored races, 164 boxes; nothing else is near 0.94. Not a wrap artefact - progress runs 2.9106 to 2.9946 straight through it, and 0.177 of a lap is about 4.4s of driving on a 25s lap. Box to hold was 3.567s, the normal human roulette. So the game runs a roulette for a road pickup and writes it to the same fields, which is the second of the two branches left open on 2026-09-03.
- `2026-09-04`: both off-row boxes are the human, slot 0, out of a field that is ten twelfths CPU. That is what a person swerving for a dropped item looks like and a CPU does not do; it is also only two events, so it is offered as evidence and not as proof. The dashboard's threshold is 0.08 of a lap, which keeps the Ghost Valley one and drops the Gold Mine one at 0.054 - near enough to a wide row to be arguable, and this is a claim about what somebody did.
- `2026-09-04`: CORRECTION -> the "0 scavenged" line earlier today was measured with a rule that could not have found one. It looked for a `hold` with no `box`, which only catches a pickup the game hands over *without* a roulette; the evidence above says it runs one. Both rules are now in `stats.ts::scavengedBy` and the counts stand: 715 holds all follow a box, 695 uses all follow a hold, so the no-box branch has still never fired.
- `2026-09-04`: **the Star on Mario Circuit is still not in the log**, checked a third way. All 64 boxes in that race sit in the course's four rows (0.06, 0.22, 0.68, 0.78); Birdo's seven are all deep inside one. He held nothing between the Red Shell he used at 31.650 and the box at 42.783, which is the window the Star slot 4 dropped at 33.267 was on the road. So that pickup wrote nothing at all - which the widget says on its face rather than claiming nobody scavenges.
- `2026-09-04`: reported by James -> items go along the bottom of the grid at full width, so a player's whole line of items is on one row instead of the six that fit in a third of one; hit by, who hit who and the scavenger sit beside each other above it. The replay's speed defaults to the race's own clock, labelled "timed", rather than 2x.
- `2026-09-04`: a decision, not a failure -> **the scavenger widget is removed from the dashboard.** James, having seen it: if there is no way to get scavenge stats, take the box out. The method stands and is the two entries above - a pickup off the road is written exactly like an item box, and the only tell is that it happened more than 0.08 of a lap from every other box in the race. It gives one candidate in the whole corpus, and one event that appears on one session's screen and nowhere else is not a stat. `stats.ts::scavengedBy`, `ScavengedStrip`, `ScavengedList` and `ScavengedNote` were deleted; commit `7ae7050` has them if this is ever revived, and the thing that would justify reviving it is a recording of somebody driving over a dropped Star on purpose, which is still the Open item.
- `2026-09-04`: with the box gone, items / hit by / who hit who are the row of three under the chart, and the replay defaults to 4x - James's correction to the 1x default earlier today, which he did not ask for: "I meant to default to four times speed."
- `2026-09-04`: reported by James -> **road the trace threw away is now drawn**, pale, under the road, in every view rather than only under Check. He was setting start lines and drawing laps over sections he could not see, and on Grumble Volcano the start line is on one of them. `tools/build_tracks.py` emits a second path, `rest`: every component of ink in the drawing that is not one of the pieces the lap runs through, down to 50px. Grumble Volcano's is 12,083px, the biggest component in the drawing, dropped for being 40.3px across against the 30px limit and encircling nothing; Mushroom Gorge's is the 18 bouncy mushrooms at 120-180px each, under the 300px floor and outside the eight biggest components. It is deliberately not merged into `outline`, because what lands in it is not decided - DS Peach Gardens' hedges are in it too - so it is drawn as scenery and never as road.
- `2026-09-04`: rebuilding all 32 with the new field changed no `d` and no `outline` line in `tracks.ts` - 41 added lines, 0 removed - so the start lines and drawn laps James set against them did not move. That was the thing to check before running the tool: `starts.json` stores a point and `oriented()` snaps it to the nearest point on the path, so a path that shifted would have moved every start line with it.
- `2026-09-04`: reported by James -> **Grumble Volcano's start line is on a stretch the lap did not run through**, and drawing the pale road was not enough on its own. Rendered the drawing with the kept road, the dropped road, the traced lap and the stored start on it (`scratchpad`, throwaway): the lap had a break in the left-hand straight, and his click sat in the break, 9.4px from anything on the path. Cause: `trace()` drops a piece whose route through it is under 40px, and that straight traces 39. Floor lowered to 30 (`MIN_ROUTE`); 30, 25 and 20 all give the same 32 traces, so the figure is doing no work beyond being under 39.
- `2026-09-04`: three courses changed and no others, measured by re-tracing all 32 and diffing `tracks.ts`: Grumble Volcano 4 -> 5 pieces, lap 649 -> 693px; Rainbow Road 5 -> 6, 872 -> 918px, covers 0.99 -> 1.00; GBA Shy Guy Beach 4 -> 5, 578 -> 685px, **covers 0.80 -> 0.91** - the top-left corner it used to cut is now in the lap, which was an Open item on 2026-09-03. The check that mattered was the start lines, since `starts.json` stores a point and `oriented()` snaps it: measured against James's own file, Grumble goes 9.4px -> 1.5px from where he clicked, Rainbow Road 1.1 -> 0.7, Shy Guy Beach 1.7 -> 1.8, every other course unchanged. Worst across all 32 is 6.0px, on Wario's Gold Mine, where the click itself is 6px off the road.
- `2026-09-16`: **the dashboard is a website.** James's direction: host it, and since a website cannot read `races/`, let a session be loaded as files. `npm run build` now gives a static site (art copied in, Vite's bundles moved to `dist/app/`), and without a server the stats page takes a dropped or picked session folder, keeps it in IndexedDB and computes everything in the browser with the unchanged `stats.ts`. Checked against a static server with no `/api`: `amy-2-player` loaded from its six files gives the same leaderboard as the local dashboard (Birdo 60, Toad 15), survives a reload, opens a race and its replay from a fresh tab, and saves a name from Who is who. Loading `races/`-style paths for two sessions at once gave two sessions, races ordered by file name where there was no `session.json`, and a non-race `.jsonl` reported as skipped. No horizontal scroll at 375px on any page. The site also has a front page, a setup guide, the build story and dated updates, as Markdown in `web/content/`.
- `2026-09-16`: a decision, not a failure -> **the site ships with no races in it.** They are the players' own nights and James has not said to publish any; a visitor loads their own. Bundling one is a copy of a session folder into the build and a link to it, if that changes.
- `2026-09-16`: a decision, not a failure -> **the `#/tracks` editor stays local-only.** Its saves are PUTs to the local server, which a static host does not have; the links to it are hidden when `/api/sessions` is not JSON, and the layout drawings it shows are left out of the build. Making it work hosted would mean saving start lines and drawn laps in the browser and exporting them as `starts.json` / `routes.json` to commit.
- `2026-09-16`: the Dolphin settings on the setup page were read off this Mac's Dolphin config, not remembered: `WiimoteNew.ini` has `Source = 2` (real Wii Remote) on all four, `Dolphin.ini` has `WiimoteContinuousScanning = True`, `SIDevice0-3 = 6` (GameCube Adapter for Wii U) and `DebugModeEnabled = True`. It also has `EnableCheats = True`, which the page leaves out because nothing here says what it was for.
- `2026-09-16`: SUPERSEDES the "site ships with no races" decision above, James -> **one night is published as an example**: `cheeky-16-sept-15th-26`, copied verbatim into `web/public/examples/` (session.json and both race logs, 326 kB; no players.json, so it names characters, not people). "See an example night" on the front page, and under Load races when there is no server, loads it through the same `loadFiles` as a dropped folder. Checked on the static build: its leaderboard reads the same as the local dashboard over `races/` (Toadette 27, Birdo 23, Daisy 20), and the replay opens. Visitors still load their own by default; the example link is hidden locally, where there are real races.
- `2026-09-16`: setup page now starts with installing the Homebrew Channel (Wilbrand + HackMii, Wii Menu 3.0-4.3), taken from the Wii hacks guide's own source (`hacks-guide/Guide_Wii`, `wilbrand.md` and `hbc.md`), and the Mii step uses Dolphin's SD sync folder as that guide's `homebrew-dolphin.md` describes. CleanRip and SaveGame Manager GX link to their Open Shop Channel pages, both checked to exist. Also fixed: the session picker was 420px wide on a 375px phone with a long session name, pushing the page sideways; it is now capped at the width of the screen.
- `2026-09-16`: CORRECTION, James -> **the example night is `cheeky-12-sept-15th-26`, not `cheeky-16`.** `cheeky-16` was the wrong session; it is off the site, still in `races/`, and renamed there to `20260915-193724-cheeky-2-sept-15th-26` (directory and `session.json` name) because it holds two races, not sixteen. `web/public/examples/` now has cheeky-12's `session.json` and all twelve race logs, 1.5 MB, copied verbatim. Its `players.json` is left out: it names four real people, and nobody said to publish names, so the site shows characters. Checked on the static build: the loaded example's leaderboard matches the local dashboard over `races/` (Birdo 120, Baby Daisy 119, Daisy 118, Baby Mario 117). The four humans drive the same character in all twelve races, so without names they still group into the same four players.
- `2026-09-16`: setup page, James -> it did not say that CleanRip and SaveGame Manager GX have to be downloaded on the Mac and copied to the SD card before the Homebrew Channel can run them. Now its own step, with a short "how the Wii part works" above the steps. The ripping steps follow the Wii hacks guide's `dump-games.md` (the CleanRip zip has an `apps` folder to copy to the card root; the answers to CleanRip's questions) and `backups.md` (join the `.partX.iso` files with `cat`, check the MD5), replacing the placeholder part names.
- `2026-09-16`: SUPERSEDES the "players.json is left out" part of the cheeky-12 line above, James -> **the example night ships with its names** ("I think my friends will be okay with that"). `players.json` copied verbatim into `web/public/examples/cheeky-12-sept-15th-26/`, and `loadExample` fetches it. A copy already cached in a browser without players is loaded again. Checked on the static build (`vite preview`): Names shows James (Birdo) 120, Wang 119, Seb 118, Shikhar 117.
- `2026-09-16`: PostHog on the hosted site, production builds only. Region found by posting the project key to `/flags/?v=2` on both clouds: EU 200, US 401. First build sent one `$pageview` and nothing on navigation: `defaults: "2026-08-30"` makes `capture_pageview` `'history_change'`, which posthog-js reads as path changes only, and also strips the hash from `$current_url`. Every route here is a hash, so both are set explicitly (`capture_pageview: { path, hash }`, `disable_capture_url_hashes: false`). Checked with `ph_debug` on the build: `#/setup` then `#/journey` each logged a `$pageview` send.
- `2026-09-16`: mobile fixes, measured at 375 px by comparing each expanded panel's `scrollWidth` to `clientWidth`. Blue shells list overflowed by 46 px and the race page's hit log by 60 px (rows that never wrapped); both now wrap on a phone, and are 0. The expanded panel was also see-through: `.card` sat outside any layer, so its `bg-white/85` beat the dialog's `bg-white` utility. `.card` is now in `@layer components`. Home page faces were `object-cover` in a circle, which cropped the square renders; now `object-contain` with padding.
